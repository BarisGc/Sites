function main() {
  const N = parseInt(readLine().trim(), 10);
  if (N % 2 == 0) {
    if (n > 1 && n < 6) {
      console.log("Not Weird");
    } else if (n > 5 && n < 21) {
      console.log("Weird");
    }
  } else {
    console.log("Weird");
  }
}
